{% extends "base.html.twig" %}

{% block title %}
	{{ recipe.title }}
{% endblock %}

{% block body %}

	<div class="row">
		<div class="col-md-8">
			<h2>{{ recipe.title }}</h2>
			<p>{{ recipe.content }}</p>
			<p class="text-primary">
				{{ recipe.duration }}
				min</p>
		</div>
	</div>

{% endblock %}
